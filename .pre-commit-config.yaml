repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # ! Ruff Version
    rev: v0.9.0
    hooks:
      # ! Ruff linter
      - id: ruff
        args: [--fix]
      # ! Ruff formatter
      - id: ruff-format